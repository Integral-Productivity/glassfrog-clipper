/**
 * The structured path: same capture, more of it revealed. Per STRATEGY.md this
 * must be the same path with more surface — not a second, separate flow.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent = 'GlassFrog Clipper';
}
